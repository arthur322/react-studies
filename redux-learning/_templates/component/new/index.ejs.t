---
to: src/components/<%= name %>/index.js
---
import React from 'react'

import { Styled<%= name %> } from './styled'

const <%= name %> = () => {
  return (
    <Styled<%= name %>>
      
    </Styled<%= name %>>
  )
}

export default <%= name %>
